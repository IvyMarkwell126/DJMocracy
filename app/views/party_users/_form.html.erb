<%= form_with(url: party_users_path, local: true) do |form| %>
    <% if party_user.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(party_user.errors.count, "error") %> prohibited this party_user from being saved:</h2>

            <ul>
                <% party_user.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <%= form.hidden_field :user_id, :value => params[:user_id] %>

    <div class="field">
        <%= form.label :party_id %>
        <%= form.text_field :party_id, id: :party_user_party_id %>
    </div>

    <div class="actions">
        <%= form.submit 'Join this party' %>
    </div>
<% end %>
